import React from 'react'
import ReactDOM from 'react-dom/client'
import { VoiceAIWrap } from './VoiceAIWrap.tsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <VoiceAIWrap />
  </React.StrictMode>,
)
